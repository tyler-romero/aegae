[project]
name = "aegae"
version = "0.1.0"
description = "Custom Triton Kernels"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
